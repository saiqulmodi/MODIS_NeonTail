# MODIS_NeonTail — "Neon Tail & The Thermal Tracker"
*Project-specific reference — save as `CLAUDE.md` in this project's own folder (`C:\Users\saiqu\Projects\MODIS_NeonTail\CLAUDE.md`) so any Claude Code session opened here reads it automatically. As of 2026-09-19.*

## What this project is

A polished 2D game, built as the user's **first real coding product**, teaching them Python/pygame-ce game development from scratch through the terminal. Chor Police (tag) game, non-violent, built for both young kids (the user's sons Sahid and Sahil) and hardcore game players — easy to learn, hard to master.

**Chor** = Agent S.Q.U.I.R.E.L. — a nimble squirrel stealing glowing neon acorns.
**Police** = V.I.P.E.R. — a hovering snake-drone security robot with heat vision.
A caught player gets beamed into a comical "stasis bubble" timeout — never anything violent.

Modes: Player vs AI (default), local 2-player (shared keyboard), timed rounds with role swap.

## How this project runs — originally STRICT teaching mode, changed by explicit user request during Phase 3

The user's ORIGINAL rules for this project (their own words, condensed) — still apply to explanation style and pacing, but see the "changed" note below for execution:
- **Before each phase**: confirm project name, exact file(s) to edit, and the exact next step. Then **wait for their "go"**.
- **ONE step per message.** Never bundle multiple steps.
- Explain new concepts in 2-3 simple sentences, no jargon dumps.
- If something errors: **ask for the full error text and fix only that** — don't guess ahead.
- Free/self-generated assets only (simple shapes first, real art later). Never copyrighted characters or music.
- 100% non-violent, age-appropriate at every level.

**CHANGED, 2026-09-19, explicit user request** ("CAN ETIRE THINGS" → clarified as "DO IT FOR ME WITHOUT ASKEING TO PASTE" / "DO FOR ME"): Claude now writes files and runs terminal commands (git add/commit/log, syntax checks) directly, using its own tools, instead of asking the user to paste content into Notepad or copy-paste command results back. This followed real, repeated friction: Notepad was silently corrupting pasted code (see Phase 2 in the status log below), and the user was often pasting command TEXT back instead of command RESULTS, needing several rounds of clarification each time.

**What's still the user's job**: actually running `python main.py` and describing what they see/how it plays — Claude has no way to view their screen or press keys in the game window, so all gameplay/visual verification still comes from the user's own testing and description. Everything else (writing code, git operations, syntax checks) is now Claude's job, reported back rather than delegated.

**Tooling note carried over from earlier friction**: on this machine, Python processes spawned via the Bash tool have an unreliable view of the filesystem (a file just written can appear via `ls` but fail to open when a Bash-spawned `python` tries to read it). Use the PowerShell tool for verification/syntax-checks and for git commands in this project, not Bash.

## Project facts

- **Folder**: `C:\Users\saiqu\Projects\MODIS_NeonTail`
- **Engine**: Python 3.12 (via `py -3.12`) + pygame-ce 2.5.8, pinned exactly in `requirements.txt`
- **Venv**: `venv/` (gitignored)
- **Git**: initialized, `master` branch, pushed to GitHub at `https://github.com/saiqulmodi/MODIS_NeonTail` (no separate tagged Release was requested). Commits: `01f3913` (Phase 1), `c474607` (Phase 2), `fdf8ad9` (Phase 3), `6856a0e` (CLAUDE.md added), `3935fc9` (Phase 4 Step 1), `53b0460` (Phase 4 Step 2), `b93a797` (gitignore `__pycache__/`), `619c3b1` (CLAUDE.md status update), `bc69e7e` (Phase 5 Step 1), `a3418ac` (Phase 5 Step 2), `2c537bc` (CLAUDE.md status update), `6f78d99` (Phase 6 Step 1), `2f8de67` (Phase 6 Step 2), `9701684` (Phase 6 Step 3), `9346ae2` (CLAUDE.md status update), `c9fe626` (Phase 7 Step 1), `e6832af` (Phase 7 Step 2), `985a3b3` (Phase 7 Step 3), `9cc3d9f` (Phase 7 Step 4), `c4e4714` (CLAUDE.md status update), `72085eb` (Phase 8 Step 1), `e5c59e8` (Phase 8 Step 2), `b5f00ac` (Phase 8 Step 3), `5013852` (Phase 8 Step 4), `8d7f951` (CLAUDE.md status update), `dada89c` (Phase 9 Step 1), `8600789` (Phase 9 Step 2), `a494644` (Phase 9 Step 3), `493f2ea` (Phase 9 Step 4), `7b3a0e1` (Phase 9 Step 5), `da970ab` (CLAUDE.md status update), `9a03d95` (Phase 10 Step 1), `dcc4805` (Phase 10 Step 2), `e43c171` (Phase 10 Step 3), `b2fd107` (Phase 10 Step 4), `2033bf7` (Phase 10 Step 5), `e53e07e` (Phase 10 Step 6), `dcfeac0` (CLAUDE.md status update), `aafadf3` (Phase 11 Step 1), `ee5bc92` (Phase 11 Step 2), `68a50c6` (Phase 11 Step 3), `df3765b` (role-swap control fix), `11f5a4a` (Phase 11 Step 4), `5edc498` (CLAUDE.md status update), `e5d7fb7` (Phase 12 Step 2), `8887452` (Phase 12 Step 3), `b7d75ec` (Phase 12 Step 4)

## Build phases (12 total, one step at a time, do not skip ahead)

1. **Setup** — venv, requirements.txt, git init, window opens with a colored background — **DONE, committed (`01f3913`)**
2. Squirrel moves with keyboard, animated placeholder sprite — **DONE, committed (`c474607`)**
3. V.I.P.E.R. drone with basic chase AI — **DONE, committed (`fdf8ad9`)**
4. Tag mechanic, stasis bubble timeout, score, timer — **DONE, committed (`3935fc9`, `53b0460`)**
5. Dirt Kick particle effects, drone blinding — **DONE, committed (`bc69e7e`, `a3418ac`)**
6. Tile-map level loader from JSON, levels 1-10 — **DONE, committed (`6f78d99`, `2f8de67`, `9701684`)**
7. Menus, level select, pause, save system — **DONE, committed (`c9fe626`, `e6832af`, `985a3b3`, `9cc3d9f`)**
8. Sound, music, taunt bubbles, whoopee-cushion landmines — **DONE, committed (`72085eb`, `e5c59e8`, `b5f00ac`, `5013852`)**
9. Levels 11-50: jump-pads, Tail Flagging, Tracker Tags, shields — **DONE, committed (`dada89c`, `8600789`, `a494644`, `493f2ea`, `7b3a0e1`)**
10. Levels 51-100: gravity flips, time bubbles, tripwires, teleport dodge, predictive AI — **DONE, committed (`9a03d95`, `dcc4805`, `e43c171`, `b2fd107`, `2033bf7`, `e53e07e`)**
11. Local 2-player mode and gamepad — **DONE, committed (`aafadf3`, `ee5bc92`, `68a50c6`, `df3765b`, `11f5a4a`)**
12. Polish, playtest balance, pytest tests, PyInstaller .exe, README, GitHub release — **DONE, committed (`e5d7fb7`, `8887452`, `b7d75ec`)**, pushed to GitHub

## Project status: COMPLETE

All 12 phases are done. The game is fully playable, tested, packaged, documented, and pushed to GitHub. Any further work from here is enhancement/maintenance on a finished product, not part of the original build plan.

## Planned folder structure (not all built yet)

```
neontail/
  main.py, settings.py
  assets/ (images, sounds, fonts)
  levels/ (level_001.json ...)
  src/ (game.py, scenes/, entities/, ai/, systems/, ui/)
  tests/
  requirements.txt, README.md
```

## Techniques to be introduced across phases (explain briefly, in-context, when first used)

OOP entity classes (Squirrel, ViperDrone, Acorn, Decoy, TrackerTag, Landmine, Level, Game); fixed-timestep 60 FPS game loop; scene state machine (Menu, LevelSelect, Playing, Paused, LevelComplete, GameOver); rebindable input + gamepad; rect/circle/mask collision + tile maps; particle system; AI (vision cones, heat detection, patrol/chase/search, A* predictive tracking, difficulty curve); JSON-driven levels; pygame.mixer sound/music; screen shake, tweening, sprite sheets; JSON save system; kid-friendly assist mode; pytest for AI/level loading; git commit after each verified phase; PyInstaller .exe packaging. (3D upgrade via Ursina/Godot is explicitly a "later, not now" idea.)

## Status log

- 2026-09-19: **Phase 1 complete and committed.** Folder created, `py -3.12 -m venv venv`, pygame-ce 2.5.8 installed and pinned in `requirements.txt`, `git init` + `.gitignore` (excludes `venv/`), `main.py` written (minimal pygame window, dark space-blue background `(20, 20, 40)`, clean quit on Escape/X, 60 FPS `clock.tick`). Verified live by the user running `python main.py` themselves — window opened with the correct color, closed cleanly with no traceback. First commit `01f3913` — "Phase 1: project setup, window opens with colored background", 3 files (`.gitignore`, `main.py`, `requirements.txt`).
  - Two real friction points hit and resolved during this phase, not hypothetical: (1) user typed `Activate.ps` instead of `Activate.ps1` — simple typo, caught from the exact PowerShell error text as instructed. (2) User pressed Ctrl+C in the terminal to try to close the running game instead of closing the actual game window (Escape/X) — produced a `KeyboardInterrupt` traceback that looked alarming but was not a code bug; the window itself had opened correctly with the right color the whole time. (3) A `git commit -m "..."` message got split across two separate pastes, landing the closing `"` on a different line than the opening one, producing a stuck PowerShell `>>` continuation prompt — resolved with Ctrl+C then a clean single-paste retry. (4) `git add` was typed once directly into the Claude Code chat (not the user's own terminal) and mistaken for having been run — caught when the following `git commit` reported "nothing added to commit"; re-run for real in PowerShell fixed it.
- 2026-09-19: **Phase 2 complete and committed.** `main.py` updated in two steps: Step 1 added keyboard movement (arrow keys/WASD, delta-time-based speed, clamped to window bounds) with a static orange placeholder rectangle for the squirrel; Step 2 added a simple `math.sin`-based hop animation while moving, settling flat when idle. Verified live twice by the user (moves correctly after Step 1; moves AND hops after Step 2). Second commit `c474607` — "Phase 2: squirrel moves with keyboard, simple hop animation".
  - **Real, significant friction point this phase — worth expecting again**: pasting multi-line code into Notepad silently corrupted it in dozens of places (lines truncated mid-word, e.g. `WINDOW_WIDTH = 1` instead of `1024`, `SQUIRREL_SPEED =` with the value entirely missing, `for even` instead of `for event in pygame.event.get():`). Not a user mistake — confirmed by reading the actual saved file content via `Get-Content main.py`, which matched the corrupted text exactly. Root cause not diagnosed (possibly a Notepad/clipboard sync issue on this machine with large pastes). **Fix that worked**: abandoned the Notepad-paste approach for this file and had Claude write the file directly instead (the user explicitly asked for this: "CAN WE DO WITH MY INTERVINE" / "WITHOUT MY INTERVINE") — the user still runs and verifies every result themselves, only the mechanical act of getting the text into the file changed. **If Notepad-paste corruption happens again on a future phase, don't retry Notepad repeatedly — switch straight to Claude writing the file directly and having the user verify via `Get-Content`/running it,** rather than burning multiple rounds on a re-paste that's likely to corrupt the same way.
  - Also hit again this phase: the user pasting a command's TEXT back as if it were the result of running it (no actual output shown), several times in a row — resolved each time by asking for the literal terminal output after the command runs, not the command itself. Worth staying patient and explicit about this distinction throughout the project, it's a recurring pattern for this user, not a one-off.
- 2026-09-19: **Phase 3 complete and committed.** `main.py` updated in two steps: Step 1 added V.I.P.E.R. as a second entity with a fixed left-right patrol (bounces between two x-bounds at a fixed height, simplest possible "AI" — no decisions, just a repeating pattern); Step 2 added real chase behavior — every frame, straight-line distance to the squirrel (`math.hypot`) is checked against `VIPER_DETECTION_RANGE` (250px); within range it moves toward the squirrel using a normalized direction vector at `VIPER_CHASE_SPEED` (220, deliberately slower than the squirrel's own 300 so a reacting player can always outrun it); outside range it resumes patrolling at a fixed height (a stated simplification — it doesn't remember/resume its exact previous patrol position, snaps back to `VIPER_PATROL_Y` instead). Verified live by the user both after Step 1 (patrol moving on its own) and Step 2 (chases when close, correctly disengages and the user could escape). Third commit `fdf8ad9` — "Phase 3: V.I.P.E.R. drone with patrol and chase AI".
  - **Operating mode changed here, explicit user request** (see "How this project runs" above for the full note) — from this point on, Claude runs git commands and writes files directly using its own tools rather than asking the user to paste content/results. This commit itself was the first one run via Claude's own PowerShell tool rather than the user's terminal. The user still does all actual gameplay testing and reports what they see, since Claude can't view the game window or press keys in it.
- 2026-09-19: **Phase 4 complete and committed.** `main.py` updated in two steps: Step 1 added the tag mechanic — when V.I.P.E.R.'s rect collides with the squirrel's rect while `squirrel_state == "free"`, the squirrel enters a `"stasis"` state (frozen, no input accepted, drawn as a pale icy-blue circle instead of the usual rectangle) for `STASIS_DURATION` (3 seconds), then returns to `"free"` and re-centers; V.I.P.E.R. keeps patrolling/chasing normally throughout, it just has nothing to chase while the squirrel is in stasis. Step 2 added an on-screen score ("Caught: N", incremented on every tag) and a running game timer ("Time: MM:SS", counting up every frame) using `pygame.font.SysFont` + `render()`/`blit()`. Verified live by the user: caught → bubble → released confirmed Step 1; both the counter and timer updating correctly confirmed Step 2. Two commits — `3935fc9` ("Phase 4 Step 1: tag mechanic with stasis bubble timeout") and `53b0460` ("Phase 4 Step 2: on-screen score and running timer") — plus `b93a797` adding `__pycache__/` to `.gitignore`.
  - **Process note**: Claude initially wrote the Step 2 (score/timer) code directly on top of the not-yet-committed Step 1 code in the same file, before committing Step 1 as the user had just asked. Caught before committing — fixed by reconstructing a Step-1-only version from the last commit's `main.py` (via `git show HEAD:main.py`), committing that alone first, then re-applying the Step 2 changes on top as a separate, later commit. Worth deliberately committing each step right after it's verified, rather than writing ahead into the next step, to avoid needing this kind of untangling again.
- 2026-09-19: **Phase 5 complete and committed.** `main.py` updated in two steps: Step 1 added a particle system — pressing Space spawns `PARTICLE_COUNT` (10) small dirt-brown dots from the squirrel's position, fired in a `PARTICLE_SPREAD_DEGREES`-wide (40°) cone around whichever direction the squirrel last moved (tracked as `facing_x`/`facing_y`, updated on every WASD/arrow press), each with a random speed and a fixed `PARTICLE_LIFETIME` (0.4s); every frame each particle's position updates and its lifetime ticks down, and a list comprehension drops any particle whose lifetime has expired. Step 2 gave V.I.P.E.R. its own `viper_state` ("active"/"blinded") alongside the squirrel's existing state: if any particle's position lands inside V.I.P.E.R.'s rect (`viper_rect.collidepoint(...)`) while it's active, it goes blinded for `VIPER_BLIND_DURATION` (3s) — drawn dim gray (`VIPER_BLINDED_COLOR`) — during which it ignores the squirrel entirely and just patrols, then reverts to normal chase/patrol behavior. This required separating the squirrel's stasis-timer countdown from V.I.P.E.R.'s movement branch (previously combined in one `if squirrel_state == "free": ... else: ...` block) so the chase condition could check both `squirrel_state == "free"` and `viper_state == "active"` together. Verified live by the user: Step 1 confirmed dirt bursts fire in the facing direction and fade cleanly; Step 2 confirmed V.I.P.E.R. turns gray and stops chasing when hit, then resumes chasing normally once the blind wears off. Two commits — `bc69e7e` ("Phase 5 Step 1: dirt kick particle system") and `a3418ac` ("Phase 5 Step 2: dirt particles blind V.I.P.E.R. on hit").
- 2026-09-19: **Phase 6 complete and committed.** Three steps, the biggest scope jump so far: Step 1 introduced a JSON level format (`levels/level_NNN.json`: `tile_size` plus a `grid` of equal-length character rows -- `#` wall, `S`/`V` start positions) and a `load_level()` function that turns a grid into `pygame.Rect` wall objects and spawn coordinates, replacing hardcoded squirrel/V.I.P.E.R. starting positions; walls are drawn but don't block movement yet. Step 2 added real collision via a new `move_with_collision(x, y, dx, dy, size, walls)` helper that resolves horizontal and vertical movement as two separate axis checks (undoing whichever axis would land inside a wall) so sliding along a wall works instead of getting stuck -- applied to the squirrel's input-driven movement and to both of V.I.P.E.R.'s movement modes (chase and patrol). Step 3 built out `level_002.json` through `level_010.json` (vertical/horizontal dividers with gaps, pillar blocks, a cross, a hollow room, a zigzag, a diagonal staircase, a serpentine triple-divider, and a combined ring+cross) via a one-off generator script (`generate_levels.py`, run from the scratchpad, not committed) that built each grid programmatically and asserted exact 12x16 dimensions and exactly one `S`/`V` each before writing -- chosen over hand-typing ~90 fixed-width grid rows, which would have been easy to silently miscount. Added an N key to cycle `level_number` (wrapping 10 -> 1) and reload the level's walls/spawns, resetting per-level state (stasis, blind, particles, facing) while deliberately leaving score and the timer running across levels. Verified live by the user at every step: walls appear and squirrel/V.I.P.E.R. spawn from the file (Step 1); walking and chasing both stop at walls and slide correctly (Step 2); pressing N cycles visibly different wall layouts with correct spawns and wraps after level 10 (Step 3). Three commits -- `6f78d99` ("Phase 6 Step 1: tile-map level loader from JSON"), `2f8de67` ("Phase 6 Step 2: per-axis wall collision for squirrel and V.I.P.E.R."), `9701684` ("Phase 6 Step 3: levels 1-10 and N-key level switching") -- plus `2c537bc` updating this status log for Phase 5.
  - **Process note**: after removing the old hardcoded `VIPER_PATROL_Y` constant in Step 1 (replaced by a per-level `viper_patrol_y` derived from the level file), one of its two usages was missed by a whitespace-sensitive find/replace (different indentation levels meant the exact-match replace only caught one of the two occurrences), leaving a reference to a now-deleted constant that would have raised `NameError` at runtime. Caught before the user tested, by reading the full file back and grepping for the deleted constant name -- worth doing that kind of grep-for-leftover-references check after any rename/removal that touches more than one spot in the file, rather than trusting a single find/replace call to have caught every instance.
- 2026-09-19: **Phase 7 complete and committed.** Four steps, introducing a proper scene state machine (`game_state`) for the first time: Step 1 added `"menu"`/`"playing"` states -- the game now opens on a title screen ("MODIS_NeonTail" + "Press ENTER to Play") instead of jumping straight into gameplay, with input/update/draw all branching on `game_state` so the menu and gameplay never run at the same time (the update section is now wrapped in `if game_state == "playing":`, and `game_time` only advances there too). Step 2 added a `"paused"` state -- Escape now pauses/resumes gameplay instead of quitting (freezing all updates while still drawing the frozen scene, with a semi-transparent black overlay via `pygame.Surface(...).set_alpha(150)` plus "PAUSED -- Esc to resume, Q to quit"); Escape still quits outright from the main menu, since there's nothing to resume there. Step 3 added a `"level_select"` state, reachable from the menu with L: Left/Right cycles a `level_select_choice` through 1-10 (wrapping both directions), Enter confirms and starts that level, Escape goes back to the menu. This introduced enough duplicated "reload a level's walls and spawn points" logic (now needed at startup, on N-key switch, and on level-select confirm) that it was pulled into a shared `reset_level(level_number)` helper before adding the third call site, rather than copy-pasting it again. Step 4 added a `save.json` file (gitignored -- player data, not source) holding `last_level` and `lifetime_catches`, read via `load_save()` at startup (falling back to defaults on a first run or a corrupt file) and written via `write_save()`/`json.dump()` any time the level changes or the squirrel gets caught; the main menu now shows "Last played: Level N   Lifetime catches: N". Verified live by the user at every step: menu blocks gameplay until Enter (Step 1); pause freezes and resumes cleanly, quit only works from the pause screen or window close (Step 2); level select cycles and starts levels correctly, Escape backs out without quitting (Step 3); catches and level switches persist across a full quit-and-relaunch (Step 4). Four commits -- `c9fe626` ("Phase 7 Step 1: menu/playing scene state machine"), `e6832af` ("Phase 7 Step 2: pause screen (Escape pauses/resumes, Q quits)"), `985a3b3` ("Phase 7 Step 3: level select screen"), `9cc3d9f` ("Phase 7 Step 4: save system (last level, lifetime catches)") -- plus `9346ae2` updating this status log for Phase 6.
  - **Process note**: Step 1's first draft used `continue` to skip straight past the update-and-draw section whenever `game_state != "playing"`, which would have skipped drawing the menu screen entirely (blank window). Caught before the user tested, by re-reading the change immediately after making it -- the fix was to gate only the *update* logic behind `if game_state == "playing":` and let drawing branch on `game_state` separately, since the menu still needs to draw something every frame even though nothing in it updates.
- 2026-09-19: **Phase 8 complete and committed.** Four steps, all audio/effects self-generated to satisfy the project's "free/self-generated assets only" rule rather than downloaded: Step 1 added `pygame.mixer.Sound` effects (a descending "womp womp" on catch, a soft poof on dirt kick, a rising "boing" on V.I.P.E.R. blind), each synthesized from raw sine/noise samples via a scratchpad script (`generate_sounds.py`, not committed) using only the standard-library `wave`/`math`/`struct` modules -- no numpy, no external audio. Step 2 added a looping background music track via the separate `pygame.mixer.music` streaming channel (distinct from `Sound`, built for one long track rather than short overlapping effects): a synthesized 16-note pentatonic arpeggio (`generate_music.py`, not committed) that starts on entering gameplay (`play(loops=-1)`) and pauses/resumes (`pause()`/`unpause()`) alongside the pause screen. Step 3 added taunt bubbles -- a random comic phrase (e.g. "Nyah nyah!") appears above the squirrel on every V.I.P.E.R. blind, using the same list-of-dicts-with-an-age pattern as the dirt particles plus `Surface.set_alpha()` (already used for the pause overlay) to fade the rendered text out smoothly instead of it just vanishing. Step 4 added whoopee-cushion landmines: a new `W` level tile (parsed by `load_level()`/`reset_level()` alongside walls and spawns, both of which gained a `landmines` return value threaded through all three call sites), drawn as pink discs; V.I.P.E.R. stepping on one (`viper_rect.collidelist(landmines)`) removes that landmine, plays a synthesized "boop-boop" (`generate_landmine_sound.py`, not committed), and sets a new `"stunned"` `viper_state` that freezes it completely for `VIPER_STUN_DURATION` (2s) -- stricter than `"blinded"`, which still patrols; two landmines were added to `levels/level_001.json` by hand for testing. Verified live by the user at every step: all three effects play correctly and sound clean (Step 1); music loops during gameplay and pauses/resumes with the pause screen (Step 2); taunt bubbles pop, drift, and fade on blind (Step 3); landmines stun V.I.P.E.R. with sound, are used up correctly, and reset on level switch (Step 4). Four commits -- `72085eb` ("Phase 8 Step 1: sound effects (caught, dirt kick, blind)"), `e5c59e8` ("Phase 8 Step 2: looping background music"), `b5f00ac` ("Phase 8 Step 3: taunt bubbles on V.I.P.E.R. blind"), `5013852` ("Phase 8 Step 4: whoopee-cushion landmines") -- plus `c4e4714` updating this status log for Phase 7.
- 2026-09-19: **Phase 9 complete and committed.** Five steps, the largest single phase so far, starting with two mechanic designs Claude asked the user to confirm rather than guess at (see below): Step 1 added jump-pads -- a new `J` level tile (walls/landmines/spawns' loading trio gained a `jump_pads` return value, again threaded through `load_level()`, `reset_level()`, and all three level-load call sites) that launches the squirrel `JUMP_DISTANCE` (150px) in its current facing direction when stepped on, still resolved through `move_with_collision()` so it can't launch through a wall; unlike landmines it's reusable, gated only by a short `jump_cooldown_timer` rather than being removed. Step 2 added Tail Flagging as a decoy drop (F key): a single `decoy` dict (position + countdown timer) that V.I.P.E.R. targets instead of the real squirrel whenever it's within detection range of the decoy, consumed on contact or expiring after `DECOY_DURATION` (5s). Step 3 added Tracker Tags: `tag_timer` resets to `TAG_DURATION` (4s) every frame V.I.P.E.R. is within detection range of the real squirrel and only counts down once that stops being true, so leaving detection range no longer instantly breaks a chase -- the first timer in the game that extends a behavior on refresh rather than just gating one from the moment it's set. Step 4 added shields: a new `H` tile pickup (`shields`, a fifth per-level list, now threaded through the same three call sites) grants `has_shield`; the next catch consumes it instead of triggering stasis, followed by a brief `invulnerable_timer` window so the same overlapping frame can't immediately catch you again. Step 5 generated `level_011.json` through `level_050.json` (40 levels) via a scratchpad script (`generate_more_levels.py`, not committed) that reused Phase 6's "build a grid programmatically, assert 12x16 and exactly one S/V, write it" approach, picking one of ten wall-layout recipes (variations on Phase 6's dividers/pillars/cross/hollow-room/zigzag/staircase/serpentine/ring-cross, plus a new scattered-blocks recipe) per level via `random.Random(level_number)` for reproducibility, then scattering jump-pads/shields/landmines onto open floor tiles; `LEVEL_COUNT` bumped from 10 to 50. Verified live by the user at every step: jump-pads launch correctly and don't double-fire (Step 1); decoys redirect V.I.P.E.R. and get consumed on contact or expire on their own (Step 2); V.I.P.E.R. keeps chasing briefly after losing direct range (Step 3); a shield absorbs one catch with visible feedback before/after (Step 4); levels 11-50 all load and play sensibly, spot-checked including level 50 specifically (Step 5). Five commits -- `dada89c` ("Phase 9 Step 1: jump-pads"), `8600789` ("Phase 9 Step 2: Tail Flagging decoy drop"), `a494644` ("Phase 9 Step 3: Tracker Tags (sticky lock-on)"), `493f2ea` ("Phase 9 Step 4: shields"), `7b3a0e1` ("Phase 9 Step 5: generate levels 11-50") -- plus `8d7f951` updating this status log for Phase 8.
- 2026-09-20: **Phase 10 complete and committed.** Six steps, opening with another `AskUserQuestion` round (this time four mechanics, including "gravity flips," which doesn't literally apply since the game has no jump/vertical physics -- resolved as a reversed-controls zone) before writing any code. Step 1 added predictive AI: V.I.P.E.R. now aims `PREDICTION_TIME` (0.3s) ahead of the squirrel's current velocity (recovered from that frame's already-delta_time-scaled `move_dx`/`move_dy`) instead of its exact position -- a deliberately simple stand-in for pathfinding, since the game's continuous pixel movement has no grid to run A* on. Step 2 added time bubbles (`B` tile) that slow V.I.P.E.R. to `TIME_BUBBLE_SLOW_FACTOR` (30%) while it's inside one; this was also the point where `load_level()`/`reset_level()` were refactored from a positional tuple (which had grown to 9 values across 3 call sites) into a single dict, specifically to avoid another VIPER_PATROL_Y-style mismatch as more per-level lists kept getting added. Step 3 added gravity flip zones (`G` tile): standing in one reverses the squirrel's movement and facing, computed against its position entering the frame and only applied on an actual key press so a stale facing value never gets double-flipped while standing still. Step 4 added tripwires (`X` tile): crossing one sets `tag_timer` to `TAG_DURATION` regardless of real distance, reusing Phase 9's Tracker Tag lock-on rather than inventing a second alert system. Step 5 added teleport dodge (T key): an always-available, player-triggered instant teleport (`TELEPORT_DISTANCE` 200px, resolved through the same `move_with_collision()` as the jump-pad) on a much longer `TELEPORT_COOLDOWN` (6s) than the reusable jump-pad, with a "Dodge: Ready / Xs" readout added to the on-screen HUD. Step 6 generated `level_051.json` through `level_100.json` (50 levels) with the same recipe-based generator approach as Phase 9, extended to scatter the three new zone types alongside the existing pickups; `LEVEL_COUNT` bumped from 50 to 100. Verified live by the user at every step, though Step 2's testing hit two real false alarms worth remembering: (1) a `KeyboardInterrupt` traceback from pressing Ctrl+C in the terminal (same non-bug as Phase 1) was mistaken for a real crash, and (2) the squirrel appearing to "vanish forever" at a corner turned out to be the user's own PowerShell terminal window sitting on top of that part of the game window on their desktop, not a code issue -- moving/resizing the terminal resolved it. Six commits -- `9a03d95` ("Phase 10 Step 1: predictive AI targeting"), `dcc4805` ("Phase 10 Step 2: time bubbles, refactor level data to a dict"), `e43c171` ("Phase 10 Step 3: gravity flip zones"), `b2fd107` ("Phase 10 Step 4: tripwires"), `2033bf7` ("Phase 10 Step 5: teleport dodge"), `e53e07e` ("Phase 10 Step 6: generate levels 51-100") -- plus `da970ab` updating this status log for Phase 9.
- 2026-09-20: **Phase 11 complete and committed.** Started with an `AskUserQuestion` on scope (basic 2-player vs. a full round-and-swap match), since the project's original "Modes" list separated "local 2-player" from "timed rounds with role swap" but Phase 11's own title only named the former -- the user asked for the full version. Step 1 added `game_mode` ("vs_ai"/"two_player") and replaced V.I.P.E.R.'s entire AI decision block with direct keyboard input when in two_player mode -- Player A (arrows) squirrel, Player B (WASD) V.I.P.E.R., no round timer yet; V.I.P.E.R. still freezes on stun/blind and slows in a time bubble either way, since those checks don't care whether a human or the AI is driving. Step 2 added `ROUND_DURATION`-long rounds with `match_round` (1 or 2) and `round_catches`, reusing `score` as "catches this round" and resetting the level fresh at the swap for fairness. Step 3 added a `"match_over"` game_state comparing `round_catches[1]` (Player A's viper-catches, from round 2) against `round_catches[0]` (Player B's, from round 1) and announcing a winner or a tie. Step 4 added gamepad support: `pygame.joystick` reads one controller's stick/D-pad into `arrow_left/right/up/down` alongside the keyboard, so it automatically follows whichever character Player A currently controls. Verified live by the user at every step, with `ROUND_DURATION` temporarily lowered for faster testing (10s, then 30s, then 15s at different points) and always restored to 45.0 before each commit. Five commits -- `aafadf3` ("Phase 11 Step 1: basic 2-player mode"), `ee5bc92` ("Phase 11 Step 2: round timer and role swap"), `68a50c6` ("Phase 11 Step 3: match-over winner screen"), `df3765b` (a same-day bug-fix commit, see below), `11f5a4a` ("Phase 11 Step 4: gamepad support") -- plus `dcfeac0` updating this status log for Phase 10.
- 2026-09-20: **Phase 12 complete -- the final phase, project now COMPLETE.** Step 1 was a direct question to the user rather than code: "does anything feel too easy/hard/fast/slow after all this testing?" Answer was no, so no balance changes were made. Step 2 added a `tests/` pytest suite (10 tests, all passing) covering `load_level()`'s parsing of every tile type, `move_with_collision()`'s blocking and diagonal-sliding behavior, and `load_save()`/`write_save()`'s defaults/round-trip/corrupt-file handling -- including one test that loads all 100 shipped level files and asserts each has exactly one valid squirrel and V.I.P.E.R. spawn, formalizing a check that had previously only ever been run manually via scratchpad scripts. A root `conftest.py` (empty, but its presence is what makes pytest add the project root to `sys.path` so `import main` works from `tests/`) was added alongside. Step 3 packaged the game as a standalone `.exe` via PyInstaller -- this required real path-handling changes first, since a frozen app extracts bundled files (`levels/`, `assets/`) to a temporary `sys._MEIPASS` folder that doesn't exist as a normal script, and `save.json` needed to persist next to the `.exe` rather than in that temp folder that gets deleted on exit; `resource_path()` and `app_data_path()` helpers were added to handle both cases, verified by actually running the built `.exe` (including a temporary console-mode debug build to rule out a silent crash, since GUI-subsystem `.exe`s don't block the launching PowerShell shell the way `python main.py` does -- that difference in shell behavior was initially mistaken for evidence of a crash). Step 4 added `README.md` (features, controls, running from source, building the `.exe`, running tests, project structure) and, from a user question during review, a `P` key to go to the previous level -- `N` had only ever gone forward. Step 5 pushed the whole repository to a real GitHub remote (`https://github.com/saiqulmodi/MODIS_NeonTail`, added and pushed only after the user explicitly created the repo and gave Claude the URL); the user declined a separate tagged Release with the `.exe` attached, so plain commits are the final state. Three commits -- `e5d7fb7` ("Phase 12 Step 2: pytest test suite..."), `8887452` ("Phase 12 Step 3: package as a standalone .exe with PyInstaller"), `b7d75ec` ("Phase 12 Step 4: README, plus a P key to go to the previous level") -- plus `5edc498` updating this status log for Phase 11 and the `git push` itself (no commit, since pushing doesn't create one).
  - **Process note**: pip and git both print routine progress/status information to stderr on success (pip's "new release available" notice, git's "branch -> branch" push confirmation), which PowerShell 5.1 surfaces as a `NativeCommandError` even though the command fully succeeded -- worth checking the actual output content (not just "an error was reported") before concluding a pip install or git push failed.
  - **Real bug caught between Step 3 and Step 4, worth remembering**: Step 2's round swap updated the HUD text ("Round 2/2: Arrows(A) = V.I.P.E.R., WASD(B) = Squirrel") and the scoring bookkeeping correctly, but never actually changed which physical keys drove which character -- arrows always moved the squirrel and WASD always moved V.I.P.E.R., in both rounds, regardless of what the screen announced. This survived Step 2 and Step 3's live testing because the user's quick round-based tests didn't happen to focus on "does control literally swap," and the on-screen text looked right either way. Caught by Claude re-reading the movement code before starting Step 4 (gamepad), since gamepad's design -- "an alternative to whichever role Player A currently has" -- only made sense once the underlying swap was confirmed to actually happen. Fixed with `squirrel_uses_arrows`/`squirrel_uses_wasd` booleans derived from `match_round`, and a matching `if match_round == 1: ... else: ...` split on V.I.P.E.R.'s human-input side. Worth double-checking that a feature's *visible announcement* (HUD text, a log line, a status label) and its *actual behavior* agree, especially for a swap/toggle -- a correct label is not evidence the underlying state actually changed.
  - **Process note**: "Tail Flagging" and "Tracker Tags" were only ever named in the original roadmap, never concretely designed -- rather than guess and risk redoing work across 40 upcoming levels, Claude used `AskUserQuestion` before Step 1 to confirm both mechanics' behavior (decoy-drop for Tail Flagging; V.I.P.E.R.-tags-squirrel for Tracker Tags) against the `Decoy`/`TrackerTag` entity names already sitting in the techniques list. Worth doing this any time a phase's stated scope names a mechanic without defining it, rather than picking a plausible-sounding interpretation and finding out later it wasn't what was meant.
  - **Post-completion (2026-09-20): browser (WebAssembly) build with pygbag** -- `main()` is now `async` with one `await asyncio.sleep(0)` per frame (required by the browser event loop, harmless on desktop; `asyncio.run(main())` at the bottom). pygbag rejects WAV, so every sound and the music theme now also exist as `.ogg` (converted with `soundfile`, durations verified identical) and `main.py` picks `AUDIO_EXT = ".ogg" if sys.platform == "emscripten" else ".wav"` -- desktop and the PyInstaller .exe still use the WAVs. Build from a CLEAN staging folder containing only `main.py`, `levels/`, `assets/` (pygbag bundles the whole folder it is pointed at, including `venv/`), with the `.wav` files removed from the staged copy: `venv\Scripts\python.exe -m pygbag --build <folder>` -> `build/web/` (about 65 KB package; runtime is fetched from the pygbag CDN on first load). Verified for real in Chrome via a local `http.server`: menu, Level 1 with both sprites, HUD/timer, keyboard input, and OGG audio loading all work. Not verified: audio actually audible, gamepad, and progress does not persist between browser visits (`save.json` is in temporary browser storage). Browser automation cannot satisfy the browser's click-to-enable-audio gate, so a human first click is required. Commits: `b793ed6` (async main), `db3f1d9` (OGG audio) plus this docs commit.
